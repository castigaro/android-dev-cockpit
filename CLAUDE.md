# android-dev-cockpit

@AGENTS.md

Sollte der Verweis oben nicht aufgelöst werden, lies `AGENTS.md` in diesem
Ordner direkt — dort stehen Aufbau und verbindliche Regeln der Umgebung.

Projektwissen gehört **nicht** hierher (dieses Repo ist generisch und
öffentlich): Es liegt in der lokalen `CLAUDE.md` des jeweiligen App-Repos;
Notizen und Pläne unter `planning/<projekt>/` (gitignoriert).
